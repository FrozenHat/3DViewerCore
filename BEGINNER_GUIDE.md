# Руководство для начинающих: 3D Viewer Core

## Содержание
1. [Введение](#введение)
2. [Как устроен проект](#как-устроен-проект)
3. [Что такое модули и как они работают](#что-такое-модули-и-как-они-работают)
4. [Система конфигурации](#система-конфигурации)
5. [Как собрать ядро](#как-собрать-ядро)
6. [Как использовать ядро на вашем сайте](#как-использовать-ядро-на-вашем-сайте)
7. [Как изменить настройки и пересобрать](#как-изменить-настройки-и-пересобрать)
8. [Создание полнофункционального ядра](#создание-полнофункционального-ядра)

---

## Введение

**3D Viewer Core** — это библиотека для отображения 3D-моделей на веб-сайтах. Она построена на основе Three.js (популярного движка для 3D-графики в браузере) и предоставляет удобный интерфейс для работы со сложными механическими моделями.

### Ваша гипотеза — правильная! ✅

Да, вы совершенно верно понимаете концепцию:
- **Модули** — это готовые решения, которые упрощают работу с Three.js
- Вместо того чтобы каждый раз писать код для камеры, освещения, анимации и т.д., вы используете готовые модули
- Эти модули предоставляют функции в удобной форме
- Когда вы вызываете их методы, вы получаете готовый функционал без необходимости писать низкоуровневый код

---

## Как устроен проект

Проект имеет следующую структуру:

```
3DViewerCore/
├── src/                      # Исходный код
│   ├── core/                 # Ядро библиотеки
│   │   └── viewer.ts         # Главный класс Viewer
│   ├── config/               # Конфигурации
│   │   ├── defaultConfig.ts  # Настройки по умолчанию
│   │   └── presets.ts        # Готовые пресеты (темы)
│   ├── interactions/         # Модули взаимодействия
│   │   ├── controls.ts       # Управление камерой
│   │   ├── partSelection.ts  # Выбор деталей
│   │   └── cameraFocus.ts    # Фокусировка на объектах
│   ├── loaders/              # Загрузчики
│   │   └── animationLoader.ts # Загрузка анимаций
│   ├── ui/                   # Элементы интерфейса
│   │   ├── animationPanel.ts # Панель анимаций
│   │   ├── timeline.ts       # Таймлайн
│   │   └── detailCard.ts     # Карточка с деталями
│   ├── utils/                # Утилиты
│   │   └── lightingManager.ts # Управление освещением
│   ├── animations/           # Анимации
│   ├── styles/               # Стили CSS
│   ├── types/                # TypeScript типы
│   │   └── index.ts          # Определения типов
│   └── index.ts              # Главная точка входа
├── examples/                 # Примеры использования
│   ├── basic-viewer.html     # Простой пример
│   └── advanced-setup.html   # Продвинутый пример
├── dist/                     # Собранное ядро (создаётся при сборке)
│   └── bundle.js             # Готовый файл для подключения
├── package.json              # Зависимости и скрипты
├── webpack.config.js         # Настройки сборки
└── tsconfig.json             # Настройки TypeScript
```

---

## Что такое модули и как они работают

### Модули — это классы с готовым функционалом

Каждый модуль решает определённую задачу:

#### 1. **Viewer** (`core/viewer.ts`)
Главный класс, который объединяет всё вместе:
- Создаёт 3D-сцену
- Управляет рендерингом
- Координирует работу других модулей

**Пример использования:**
```javascript
const viewer = new Viewer('container');
viewer.init();
viewer.loadModel('./models/model.glb');
viewer.render();
```

#### 2. **Controls** (`interactions/controls.ts`)
Управление камерой (вращение, зум, панорамирование):
- Вместо написания кода для обработки мыши и тач-событий
- Вы просто создаёте Controls, и всё работает

#### 3. **PartSelection** (`interactions/partSelection.ts`)
Выбор деталей модели:
- Подсветка при наведении
- Выбор кликом
- Возможность получить информацию о выбранной детали

#### 4. **LightingManager** (`utils/lightingManager.ts`)
Управление освещением:
- Создание и настройка источников света
- Ambient (рассеянный), Directional (направленный), Point, Spot свет
- Поддержка HDRI (реалистичное окружение)

#### 5. **AnimationLoader** (`loaders/animationLoader.ts`)
Загрузка и управление анимациями:
- Загружает анимации из GLTF/GLB моделей
- Позволяет запускать, останавливать, переключать анимации

### Как модули упрощают работу

**Без модулей (чистый Three.js):**
```javascript
// Нужно написать ~50-100 строк кода для:
const scene = new THREE.Scene();
const camera = new THREE.PerspectiveCamera(75, width/height, 0.1, 1000);
const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setSize(window.innerWidth, window.innerHeight);
document.body.appendChild(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.dampingFactor = 0.05;
// ... ещё много кода

const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
scene.add(ambientLight);
// ... и так далее
```

**С модулями:**
```javascript
// Всего несколько строк:
const viewer = new Viewer('container');
viewer.init();
viewer.loadModel('./model.glb');
viewer.render();
```

---

## Система конфигурации

### 1. Конфигурация по умолчанию (`config/defaultConfig.ts`)

Это базовые настройки, которые применяются, если вы ничего не указали:

```typescript
export const defaultConfig: ViewerConfig = {
    containerId: 'container',        // ID HTML-элемента
    enableSelection: true,            // Включить выбор деталей
    enableUI: true,                   // Показывать интерфейс
    
    lighting: {
        ambient: {
            enabled: true,
            color: 0xffffff,          // Белый свет
            intensity: 0.6            // Яркость 60%
        },
        directional: {
            enabled: true,
            color: 0xffffff,
            intensity: 0.8,
            position: { x: 10, y: 10, z: 10 },
            castShadow: true          // Тени включены
        }
    },
    
    camera: {
        fov: 75,                      // Угол обзора
        near: 0.1,                    // Ближняя плоскость отсечения
        far: 1000,                    // Дальняя плоскость отсечения
        position: { x: 5, y: 5, z: 5 }
    },
    
    renderer: {
        antialias: true,              // Сглаживание
        shadowMap: true,              // Карта теней
        toneMapping: true             // Тональная коррекция
    }
};
```

### 2. Пресеты (`config/presets.ts`)

Готовые наборы настроек для разных сценариев:

#### **studioPreset** — студийное освещение
Мягкое, сбалансированное освещение для презентации продуктов:
```javascript
import { studioPreset } from './config/presets';

const viewer = new Viewer('container', studioPreset);
```

#### **outdoorPreset** — уличное освещение
Яркое, естественное освещение с тёплым солнечным светом:
```javascript
const viewer = new Viewer('container', outdoorPreset);
```

#### **darkPreset** — тёмная сцена
Акцентное освещение на тёмном фоне:
```javascript
const viewer = new Viewer('container', darkPreset);
```

#### **minimalPreset** — минимализм
Только рассеянный свет, без теней:
```javascript
const viewer = new Viewer('container', minimalPreset);
```

### 3. Пользовательская конфигурация

Вы можете создать свою конфигурацию, комбинируя настройки:

```javascript
const myConfig = {
    enableSelection: true,
    enableUI: true,
    
    lighting: {
        ambient: {
            enabled: true,
            color: 0xff0000,      // Красный свет
            intensity: 0.5
        },
        directional: {
            enabled: true,
            color: 0x0000ff,      // Синий направленный свет
            intensity: 1.0,
            position: { x: 0, y: 10, z: 0 },
            castShadow: true
        }
    },
    
    camera: {
        fov: 60,
        position: { x: 10, y: 5, z: 10 }
    }
};

const viewer = new Viewer('container', myConfig);
```

---

## Как собрать ядро

Ядро — это единый JavaScript-файл (`bundle.js`), который содержит всю библиотеку и который можно подключить на любой сайт.

### Шаг 1: Установка зависимостей

Первый раз после клонирования проекта:

```bash
npm install
```

Эта команда устанавливает все необходимые библиотеки (Three.js, TypeScript, Webpack и др.).

### Шаг 2: Разработка с авто-обновлением

Для разработки используйте:

```bash
npm start
```

Это запускает локальный сервер разработки:
- Автоматически пересобирает код при изменениях
- Открывает браузер на `http://localhost:8080`
- Показывает ошибки в реальном времени

### Шаг 3: Сборка финальной версии

Когда всё готово, соберите production версию:

```bash
npm run build
```

Эта команда:
1. Компилирует TypeScript в JavaScript
2. Объединяет все модули в один файл
3. Минифицирует код (удаляет пробелы, сокращает имена)
4. Создаёт файл `dist/bundle.js`

### Что происходит при сборке?

**Webpack** (инструмент сборки) делает следующее:

1. **Читает** `src/index.ts` (точка входа)
2. **Находит** все импорты и зависимости
3. **Компилирует** TypeScript → JavaScript
4. **Объединяет** все файлы в один
5. **Оптимизирует** код
6. **Создаёт** `dist/bundle.js`

Настройки сборки находятся в `webpack.config.js`:

```javascript
module.exports = {
  entry: './src/index.ts',           // Входной файл
  output: {
    filename: 'bundle.js',           // Выходной файл
    path: path.resolve(__dirname, 'dist'),
    library: {
      name: 'ThreeDViewer',          // Глобальное имя
      type: 'umd'                    // Универсальный формат модуля
    }
  }
  // ...
};
```

---

## Как использовать ядро на вашем сайте

### Вариант 1: Локальное подключение

1. Скопируйте `dist/bundle.js` в папку вашего сайта
2. Подключите в HTML:

```html
<!DOCTYPE html>
<html>
<head>
    <title>Мой 3D Viewer</title>
</head>
<body>
    <!-- Контейнер для 3D сцены -->
    <div id="viewer-container" style="width: 100%; height: 600px;"></div>
    
    <!-- Подключение библиотеки -->
    <script src="./bundle.js"></script>
    
    <!-- Ваш код -->
    <script>
        // Создаём viewer
        const viewer = new window.Viewer('viewer-container');
        
        // Инициализируем
        viewer.init();
        
        // Загружаем модель
        viewer.loadModel('./models/my-model.glb');
        
        // Запускаем рендеринг
        viewer.render();
    </script>
</body>
</html>
```

### Вариант 2: С пользовательской конфигурацией

```html
<script src="./bundle.js"></script>
<script>
    // Используем пресет
    const config = window.ViewerLib.studioPreset;
    
    // Или свои настройки
    const customConfig = {
        enableSelection: true,
        enableUI: true,
        lighting: {
            ambient: {
                enabled: true,
                color: 0xffffff,
                intensity: 0.8
            }
        }
    };
    
    const viewer = new window.Viewer('viewer-container', customConfig);
    viewer.init();
    viewer.loadModel('./model.glb');
    viewer.render();
</script>
```

### Вариант 3: Подключение как npm-пакет

Если вы публикуете ядро в npm:

```bash
npm install 3d-viewer-core
```

```javascript
import { Viewer, studioPreset } from '3d-viewer-core';

const viewer = new Viewer('container', studioPreset);
viewer.init();
viewer.loadModel('./model.glb');
viewer.render();
```

---

## Как изменить настройки и пересобрать

### Сценарий 1: Изменить настройки по умолчанию

**Файл:** `src/config/defaultConfig.ts`

1. Откройте файл
2. Измените нужные значения:

```typescript
export const defaultConfig: ViewerConfig = {
    // ...
    lighting: {
        ambient: {
            enabled: true,
            color: 0xff9999,      // Розоватый свет вместо белого
            intensity: 0.8        // Ярче
        }
    },
    camera: {
        fov: 60,                  // Уменьшенный угол обзора
        position: { x: 3, y: 3, z: 3 }  // Ближе к модели
    }
};
```

3. Пересоберите:

```bash
npm run build
```

4. Новый `dist/bundle.js` готов к использованию

### Сценарий 2: Создать новый пресет

**Файл:** `src/config/presets.ts`

Добавьте свой пресет:

```typescript
/**
 * Мой кастомный пресет
 */
export const myCustomPreset: Partial<ViewerConfig> = {
    lighting: {
        ambient: {
            enabled: true,
            color: 0x222222,
            intensity: 0.3
        },
        directional: {
            enabled: true,
            color: 0xffaa00,     // Оранжевый
            intensity: 2.0,
            position: { x: 5, y: 15, z: 5 },
            castShadow: true
        }
    },
    renderer: {
        antialias: true,
        shadowMap: true,
        toneMapping: true
    }
};
```

Экспортируйте его в `src/index.ts`:

```typescript
export { 
    studioPreset, 
    outdoorPreset, 
    darkPreset, 
    minimalPreset,
    myCustomPreset     // Добавьте сюда
} from './config/presets';
```

Пересоберите:

```bash
npm run build
```

Используйте на сайте:

```javascript
const viewer = new window.Viewer('container', window.ViewerLib.myCustomPreset);
```

### Сценарий 3: Добавить новый функционал

Например, добавить новый тип освещения:

1. Откройте `src/utils/lightingManager.ts`
2. Добавьте метод для нового типа света
3. Обновите типы в `src/types/index.ts`
4. Пересоберите проект

---

## Создание полнофункционального ядра

Если вы хотите создать ядро, которое можно просто подключить к любому сайту и настроить через конфиг-файл:

### Шаг 1: Создайте конфигурационный файл

Создайте файл `viewer-config.json` рядом с вашей HTML-страницей:

```json
{
    "containerId": "my-viewer",
    "modelUrl": "./models/engine.glb",
    "preset": "studio",
    "enableSelection": true,
    "enableUI": true,
    
    "customLighting": {
        "ambient": {
            "color": "#ffffff",
            "intensity": 0.6
        },
        "directional": {
            "color": "#ffffff",
            "intensity": 1.0,
            "position": [10, 10, 10]
        }
    }
}
```

### Шаг 2: Создайте обёртку для автоинициализации

**Файл:** `src/autoInit.ts`

```typescript
// Автоматическая инициализация из конфига
export async function initFromConfig(configUrl: string) {
    try {
        // Загружаем конфиг
        const response = await fetch(configUrl);
        const config = await response.json();
        
        // Выбираем пресет
        let viewerConfig = {};
        switch(config.preset) {
            case 'studio': viewerConfig = studioPreset; break;
            case 'outdoor': viewerConfig = outdoorPreset; break;
            case 'dark': viewerConfig = darkPreset; break;
            default: viewerConfig = defaultConfig;
        }
        
        // Применяем кастомные настройки
        if (config.customLighting) {
            viewerConfig.lighting = config.customLighting;
        }
        
        // Создаём viewer
        const viewer = new Viewer(config.containerId, viewerConfig);
        viewer.init();
        
        // Загружаем модель
        if (config.modelUrl) {
            viewer.loadModel(config.modelUrl);
        }
        
        viewer.render();
        
        return viewer;
    } catch (error) {
        console.error('Ошибка загрузки конфига:', error);
    }
}
```

Экспортируйте в `src/index.ts`:

```typescript
export { initFromConfig } from './autoInit';
```

### Шаг 3: Используйте на сайте

```html
<!DOCTYPE html>
<html>
<body>
    <div id="my-viewer"></div>
    
    <script src="./bundle.js"></script>
    <script>
        // Автоматическая инициализация из конфига
        window.ViewerLib.initFromConfig('./viewer-config.json');
    </script>
</body>
</html>
```

### Шаг 4: Для клиентов — просто меняют конфиг!

Теперь любой человек может:
1. Подключить ваш `bundle.js`
2. Создать свой `viewer-config.json`
3. Указать путь к своей модели
4. Выбрать пресет
5. Настроить освещение, камеру и т.д.

**Без написания кода!** Только через JSON-конфиг.

---

## Описание всех возможных функций

### Структура модулей по типам взаимодействия

#### 1. **Базовые функции (Core)**
- `Viewer.init()` — инициализация сцены
- `Viewer.render()` — запуск рендеринга
- `Viewer.loadModel(url)` — загрузка 3D-модели
- `Viewer.dispose()` — очистка ресурсов

#### 2. **Управление камерой (Interactions/Controls)**
- Вращение орбиты
- Зум колесом мыши
- Панорамирование правой кнопкой
- Автофокус на объект

#### 3. **Выбор деталей (Interactions/PartSelection)**
- Подсветка при наведении
- Выбор кликом
- Множественный выбор
- Получение метаданных детали

#### 4. **Анимации (Loaders/AnimationLoader)**
- Загрузка анимаций из GLTF
- Воспроизведение/пауза
- Переключение между анимациями
- Управление скоростью

#### 5. **UI-компоненты (UI)**
- **AnimationPanel** — панель управления анимациями
- **Timeline** — временная шкала
- **DetailCard** — информация о выбранной детали

#### 6. **Освещение (Utils/LightingManager)**
- Ambient light (рассеянный)
- Directional light (направленный)
- Point light (точечный)
- Spot light (прожектор)
- HDRI окружение

---

## Полный рабочий процесс

### 1. Разработка
```bash
# Клонируем проект
git clone <url>
cd 3DViewerCore

# Устанавливаем зависимости
npm install

# Запускаем dev-сервер
npm start
# Браузер откроется на http://localhost:8080
```

### 2. Внесение изменений
- Редактируйте файлы в `src/`
- Браузер автоматически обновляется
- Проверяйте результат в реальном времени

### 3. Сборка production версии
```bash
npm run build
# Создаётся dist/bundle.js
```

### 4. Тестирование
```bash
npm test
```

### 5. Развёртывание
- Копируйте `dist/bundle.js` на сервер
- Или публикуйте в npm: `npm publish`

---

## Примеры типичных задач

### Задача 1: Изменить цвет подсветки выбранных деталей

**Файл:** `src/core/viewer.ts`

Найдите строки:
```typescript
private hoverColor = new THREE.Color(0x667eea);   // Цвет при наведении
private selectColor = new THREE.Color(0x3cb371);  // Цвет при выборе
```

Измените на свои цвета:
```typescript
private hoverColor = new THREE.Color(0xff0000);   // Красный при наведении
private selectColor = new THREE.Color(0x00ff00);  // Зелёный при выборе
```

Пересоберите: `npm run build`

### Задача 2: Добавить новый тип анимации

**Файл:** `src/types/index.ts`

```typescript
export enum AnimationType {
    ASSEMBLY = 'assembly',
    OPERATION = 'operation',
    CUSTOM = 'custom',
    EXPLOSION = 'explosion'    // Новый тип
}
```

Используйте в коде для загрузки анимации взрыва.

### Задача 3: Создать тему "Neon" (неоновая)

**Файл:** `src/config/presets.ts`

```typescript
export const neonPreset: Partial<ViewerConfig> = {
    lighting: {
        ambient: {
            enabled: true,
            color: 0x000000,    // Черный фон
            intensity: 0.1
        },
        point: {
            enabled: true,
            color: 0xff00ff,    // Пурпурный неон
            intensity: 2.0,
            position: { x: 5, y: 5, z: 5 },
            distance: 50
        }
    },
    renderer: {
        antialias: true,
        shadowMap: false,
        toneMapping: true
    }
};
```

---

## Заключение

Теперь вы понимаете:

1. ✅ **Модули** — готовые решения, упрощающие работу с Three.js
2. ✅ **Сборка** — webpack объединяет всё в `bundle.js`
3. ✅ **Конфигурация** — настройки через defaultConfig и presets
4. ✅ **Изменение** — редактируете файлы в `src/`, собираете через `npm run build`
5. ✅ **Развёртывание** — подключаете `bundle.js` и конфиг на сайт

Вы можете создать полнофункциональное ядро, которое:
- Подключается одним файлом
- Настраивается через JSON-конфиг
- Имеет готовые пресеты
- Легко расширяется новыми функциями

**Следующие шаги:**
1. Изучите примеры в `examples/`
2. Поэкспериментируйте с пресетами
3. Создайте свой первый кастомный пресет
4. Попробуйте добавить новую функцию

Удачи в разработке! 🚀
